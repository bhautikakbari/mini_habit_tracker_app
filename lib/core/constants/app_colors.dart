import 'package:flutter/material.dart';

/// App-wide color constants - Dark mode only
class AppColors {
  // Primary colors - vibrant green
  static const primary = Color(0xFF4ADE80);
  static const primaryLight = Color(0xFF86EFAC);
  static const primaryDark = Color(0xFF22C55E);

  // Accent colors - vibrant blue
  static const accent = Color(0xFF38BDF8);
  static const accentLight = Color(0xFF7DD3FC);
  static const accentDark = Color(0xFF0EA5E9);

  // Dark mode specific colors
  static const background = Color(0xFF0F172A);
  static const surface = Color(0xFF1E293B);
  static const cardBackground = Color(0xFF334155);
  static const textPrimary = Color(0xFFF8FAFC);
  static const textSecondary = Color(0xFFCBD5E1);
  static const textHint = Color(0xFF94A3B8);

  // Status colors - vibrant
  static const success = Color(0xFF4ADE80);
  static const warning = Color(0xFFFBBF24);
  static const error = Color(0xFFEF4444);
  static const info = Color(0xFF38BDF8);

  // Frequency colors - vibrant
  static const daily = Color(0xFF1E3A8A);
  static const dailyText = Color(0xFF93C5FD);
  static const weekly = Color(0xFF581C87);
  static const weeklyText = Color(0xFFD8B4FE);

  // Streak colors - vibrant
  static const streakCurrent = Color(0xFFF97316);
  static const streakLongest = Color(0xFFFACC15);
}
